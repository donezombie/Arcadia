import CommonStyles from "components/CommonStyles";
import React from "react";

const Homepage = () => {
  return <CommonStyles.Container>Test</CommonStyles.Container>;
};

export default React.memo(Homepage);
